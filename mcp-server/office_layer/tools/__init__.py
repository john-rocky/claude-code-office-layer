"""MCP tool implementations live alongside ``server.py`` for Phase 0.

When the tool surface grows in Phase 2+, move each group (workspace /
indexing / search / extraction / evidence / workflows / safety) into its
own module here.
"""
